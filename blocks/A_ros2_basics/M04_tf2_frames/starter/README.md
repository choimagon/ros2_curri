# M04 Starter

- 시작 상태: M03에서 node/topic을 확인할 수 있는 상태. 아직 실제 AGV 모델은 필요 없다.
- 이전 모듈 기준: M03 node/topic 관찰
- 이 폴더의 PPT를 1번 슬라이드부터 순서대로 수행한다.
- 실제 작업 workspace는 `~/agv_ws`이고, source 원본은 이 저장소의 `agv_ws/src/`이다.
