# M18 Starter

- 시작 상태: M13 obstacle_distance와 M17 DetectionArray가 각각 topic으로 나오는 상태.
- 이전 모듈 기준: M13 + M17 outputs
- 이 폴더의 PPT를 1번 슬라이드부터 순서대로 수행한다.
- 실제 작업 workspace는 `~/ros2_curri/agv_ws`이고, source 원본은 이 저장소의 `agv_ws/src/`이다.
