# M16 Complete

PPT 완료 후의 핵심 파일 snapshot입니다. 완료 조건: `ros2 control list_controllers`에서 joint_state_broadcaster와 diff_drive_controller가 모두 active이고 wheel joint state가 publish된다.

먼저 직접 만든 workspace를 실행하고 오류를 확인하세요. 이 폴더는 막혔을 때 `diff`로 비교하거나 backup 뒤 복구하는 안전망입니다.
파일의 SHA-256 값은 상위 `CHECKSUM_or_TAG.txt`에 기록했습니다.
